# Year Fourteen

Static Vercel deployment package.

## Deploy

Upload this folder to Vercel, or run:

```sh
vercel deploy
```

The site entry point is `index.html`. The MP4 files must stay in the same folder as `index.html` because the video sources are relative paths.
